query GetProducts($first: Int, $after: String, $filter: ProductFilterInput, $sortBy: ProductOrder) {
  products(first: $first, after: $after, channel: "uk", filter: $filter, sortBy: $sortBy) {
    edges {
      node {
        id
        name
        description
        rating
        slug
        pricing {
          priceRange {
            start {
              gross {
                amount
                currency
              }
            }
          }
        }
        thumbnail(size: 300) {
          url
          alt
        }
        category {
          name
        }
      }
      cursor
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
    totalCount
  }
}
